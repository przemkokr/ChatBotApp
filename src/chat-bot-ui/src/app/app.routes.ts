import { Routes } from '@angular/router';
import { ChatBotPageComponent } from './features/chat-bot/chat-bot-page.component';

export const routes: Routes = [
  { path: '', component: ChatBotPageComponent }
];
